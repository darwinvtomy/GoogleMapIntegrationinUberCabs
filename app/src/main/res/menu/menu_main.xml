<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/next"
        android:title="@string/normal"
        android:icon="@drawable/next"
        android:onClick="showTheCities"
        app:showAsAction="always"
        />


    <item android:id="@+id/currentLocation"
        android:icon="@drawable/current_position_marker"
        android:title="@string/current_location"
        app:showAsAction="always"
        android:onClick="showCurrentLocation"/>

</menu>